# Aligned fixtures (synthetic green)

Synthetic, fully consistent contract and entity fixtures. The parity and
domain-invariant checkers must exit 0 with zero findings against this
directory. These fixtures are hand-written to demonstrate what an aligned
contract set looks like; they are not derived from production data.
