# PIBRAS Agent Instructions

Import `/Users/ronaldo/.codex/RTK.md` before running shell commands in this project.

Operational rule: prefix shell commands with `rtk`. If a command has multiple chained segments, prefix each segment.

Project scope: this repository is the PIBRAS standard workspace. Keep changes focused on the standard artifacts in `docs/`, `schema/`, `types/`, `db/`, `examples/`, `mappings/`, and `tests/golden/`.
